# Pi Project Template Changelog

All notable changes to the project-local Pi template are tracked here.

## 1.0.0 - 2026-05-22

- Added versioned project template metadata (`VERSION` and this changelog).
- Added `pi-init` sync workflow with overwrite protection and `--force` / `--dry-run`.
- Standardized template guidance to reference local `.pi/` paths.
